# SKY-BIRD environment template
# NEVER commit real credentials or DATABASE_URL.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Server-side only. Never expose this value to Vite/client code.
DATABASE_URL=
