FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=optional

COPY server ./server
COPY tsconfig.json ./tsconfig.json

ENV NODE_ENV=production
ENV ENGINE_TICK_MS=250

CMD ["npm", "run", "engine"]
