services:
  - type: worker
    name: skybird-engine
    runtime: docker
    dockerfilePath: ./Dockerfile.engine
    dockerContext: .
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ENGINE_TICK_MS
        value: "250"
